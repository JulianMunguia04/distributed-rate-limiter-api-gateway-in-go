port: 8080

services:
  - name: app1
    backends:
      - http://localhost:5000
      - http://localhost:5001
    auth_required: false

  - name: app2
    backends:
      - http://localhost:3000
      - http://localhost:3001
    auth_required: true

rate_limit:
  requests: 100
  per_seconds: 1